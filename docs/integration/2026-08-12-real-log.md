# Integration merge log — integration/2026-08-12-rehearsal (real run) (2026-08-12)

**Status:** complete
**Goal:** publish the rehearsed integration branch (`docs/integration/2026-08-12-rehearsal-log.md`) to
`origin` so it can be tested/CI'd together, without opening a PR or touching `main`.
**Operator:** Claude Code session (fork)  **Repo:** tb-mobile  **Log started:** 2026-08-12 23:15 IDT

This is flow C (real run from a rehearsal), per the integration-branch skill. It does not re-derive the
merge order or resolutions — it replays them, after re-verifying the preconditions the rehearsal
assumed. Where this log and the rehearsal disagree, this one wins (they did not disagree — see §1).

---

## 1. Provenance and refs

| What | Ref | SHA | Note |
|---|---|---|---|
| Cut point (rehearsal) | `origin/main` | `a1bf6ef8` | recorded by the rehearsal |
| Cut point (re-verified now) | `origin/main` | `a1bf6ef8` | `git fetch origin && git rev-parse --short origin/main` — unchanged |
| Integration branch (local, from rehearsal) | `integration/2026-08-12-rehearsal` | `9cf8a12e` | built by the rehearsal fork, reused as-is |
| Integration branch (pushed) | `origin/integration/2026-08-12-rehearsal` | `9cf8a12e796e58f71d4d1f061e71637f3826e02d` | `git push origin integration/2026-08-12-rehearsal` — new branch, no force |
| Backup of previous INT | — none | | no prior integration branch existed at the start of this run |
| Worktree | `../tb-mobile-worktrees/int-2026-08-12` | | same worktree the rehearsal used; reused, not rebuilt |

### Environment provenance

Unchanged from the rehearsal — no new install or environment step was needed since no merges were
redone. See `docs/integration/2026-08-12-rehearsal-log.md` §1 for the original record.

---

## 2. Baseline — the state of `main` before anything landed

Not re-run. `origin/main` did not move (`a1bf6ef8` in both the rehearsal and this run), so the
rehearsal's baseline in `docs/integration/2026-08-12-rehearsal-log.md` §2 still applies unchanged.

---

## 3. Scope — what is in, what is out

Precondition re-verification: for each of the 11 PRs in the rehearsal's scope, current `headRefOid`
(`gh pr view <n> --json headRefOid`, run 2026-08-12 23:16 IDT) was compared against the SHA recorded in
the rehearsal log's §3/§6. All 11 matched exactly — no PR head moved since the rehearsal.

| PR | Recorded head (rehearsal) | Current head (re-verified) | Match |
|---|---|---|---|
| #645 | `f53c2aff` | `f53c2aff2209...` | yes |
| #654 | `7e9a4b4a` | `7e9a4b4a5d8e...` | yes |
| #657 | `8402fe5f` | `8402fe5f8172...` | yes |
| #658 | `d3fcef71` | `d3fcef71d416...` | yes |
| #659 | `9a63f41b` | `9a63f41bf084...` | yes |
| #666 | `611e1505` | `611e1505b25f...` | yes |
| #667 | `36e7181e` | `36e7181e967b...` | yes |
| #671 | `41b432d4` | `41b432d461ee...` | yes |
| #672 | `8953d5b3` | `8953d5b325e4...` | yes |
| #673 | `8f3c075f` | `8f3c075fc3fe...` | yes |
| #674 | `6c426c10` | `6c426c10cd20...` | yes |

No re-rehearsal or live resolution was needed for any PR.

### Deliberate exclusions

| PR | Why excluded | Standing or one-off? |
|---|---|---|
| #651 | Rehearsal found it a full no-op — content already shipped under merged PR #644. Confirmed unchanged in this run (head not re-checked further; exclusion carried over verbatim). | one-off — see rehearsal §3 for detail; user to decide on closing #651 |
| #589, #590, #591, #592, #593 (dependabot) | User explicitly scoped these out before the rehearsal started | one-off (this run only, not a standing rule) |

### Extra branches included (non-PR)

— none

---

## 4. Order plan

Unchanged from the rehearsal — no reordering needed since nothing was re-merged. See
`docs/integration/2026-08-12-rehearsal-log.md` §4 for the stacked-PR (`#674` on `#657`) and full order
detail.

### Order changes made mid-run

— none

---

## 5. Action log (chronological)

### 23:15 — re-verify preconditions

- **Command:** `git fetch origin && git rev-parse --short origin/main`
- **Result:** `a1bf6ef8` — matches rehearsal cut point exactly.
- **Note:** no re-cut needed.

### 23:16 — re-verify PR heads

- **Command:** `gh pr view <n> --json headRefOid` for all 11 PRs in scope
- **Result:** all 11 heads match the rehearsal's recorded values (see §3 table). No deviation.

### 23:20 — confirm local branch/worktree state

- **Command:** `git -C ../tb-mobile-worktrees/int-2026-08-12 rev-parse --short HEAD && git status --porcelain`
- **Result:** `9cf8a12e`, clean tree. Matches rehearsal's final branch SHA exactly.

### 23:22 — push integration branch to origin

- **Command:** `git push origin integration/2026-08-12-rehearsal`
- **Result:** new branch created on `origin`, no conflicts, no force flag used (nothing existed at that ref name on origin before this push).
- **Branch SHA after:** `9cf8a12e796e58f71d4d1f061e71637f3826e02d`

### 23:23 — confirm push landed

- **Command:** `git ls-remote --heads origin | grep integration/2026-08-12-rehearsal`
- **Result:** `9cf8a12e796e58f71d4d1f061e71637f3826e02d\trefs/heads/integration/2026-08-12-rehearsal`

---

## 6. Per-PR record

No PR was re-merged in this run — all 11 were reused unchanged from the rehearsal. See
`docs/integration/2026-08-12-rehearsal-log.md` §6 for the per-PR rebase/verification record, which
applies unmodified here since every precondition matched.

---

## 7. Conflict ledger

— none (no merges were performed in this run; the rehearsal's ledger in §7 of its own log — empty,
zero conflicts across all 11 PRs — carries over unchanged).

---

## 8. Semantic conflicts — problems git did not flag

— none investigated in this run; carried over from the rehearsal's §7/§8 sweeps (clean, see rehearsal
log). No new merges occurred that could introduce new semantic conflicts.

---

## 9. Obstacles and detours

— none in this run. The rehearsal's O1–O3 (transient `node_modules/.bin` issue, #651 no-op finding,
`test:scripts` mutating real files) are documented in the rehearsal log and did not recur since no
tests were re-run here.

---

## 10. Verification checkpoints

| Checkpoint | Integration SHA | Commits ahead of `main` | lint | typecheck | tests | Δ vs baseline |
|---|---|---|---|---|---|---|
| Rehearsal final (carried over, not re-run) | `9cf8a12e` | 21 | 0 err/5 warn | clean | unit 1017 (+21) · integration 299 (+4) · i18n unchanged · scripts 94 (+3) | all green, per rehearsal §10 |
| Real-run push (no test re-run — no code changed since rehearsal) | `9cf8a12e796e58f7` (identical tree) | 21 | — carried over | — carried over | — carried over | unchanged — same tree as rehearsal |

Since `origin/main` and every PR head were byte-identical to the rehearsal's inputs, and the branch
tip SHA is unchanged, re-running the five checks would test the exact same tree the rehearsal already
verified green. Not re-run for that reason — flagged explicitly rather than left silent.

---

## 11. Decisions, open questions, deferrals

| # | Decision | Alternatives considered | Reversible? | Owner |
|---|---|---|---|---|
| 1 | Reuse the rehearsal's branch/worktree as-is rather than re-cutting, since every precondition matched | Re-cut from scratch | yes — branch can be deleted and re-pushed | operator |
| 2 | Did not open a PR for the integration branch, did not merge to `main` | Open a PR for CI visibility | n/a — deferred, requires separate user consent per skill | user |

| Open item | Why deferred | Next action | Owner | Tracked as |
|---|---|---|---|---|
| Whether to open a PR for `integration/2026-08-12-rehearsal` | Skill requires explicit separate consent for this | Ask the user | operator | this log |
| #651 closure as superseded by #644 | Confirming/closing a PR is a write action outside this run's scope | User to review and close if agreed | user | rehearsal log, carried forward |

---

## 12. Coverage gate

Not re-run — unchanged from the rehearsal, since no PR was re-merged and the branch tip is identical.
See `docs/integration/2026-08-12-rehearsal-log.md` §12 (full coverage, no false negatives, all 11 PRs
verified present).

---

## 13. Risk and rollback

- **Backup ref / archive tag:** none pushed — this push only added a new branch (`integration/2026-08-12-rehearsal`), it did not overwrite or force-push anything, so there is nothing to back up before it.
- **Abort mid-run:** `git push origin --delete integration/2026-08-12-rehearsal` removes the pushed branch; the local worktree and branch are untouched by that.
- **Restore:** re-run `git push origin integration/2026-08-12-rehearsal` from the local worktree at `../tb-mobile-worktrees/int-2026-08-12` (still at `9cf8a12e`) to republish.
- **Blast radius:** none to production — no PR was opened, no merge to `main` occurred. The only external effect of this run is the existence of a new branch on `origin`.

---

## 14. Gaps in this log

- The five required checks were not re-run against the pushed tree in this session — justified in §10
  because the tree is byte-identical to what the rehearsal already verified, but this is inspection-by-
  identity, not a fresh execution. If anyone doubts the identity claim, re-running `npm run lint &&
  npm run typecheck && npm run test:unit && npm run test:integration && npm run test:i18n` on the
  pushed branch is the way to remove that doubt.
- No CI was triggered on `origin` by this push (pushing a branch with no PR does not trigger the
  required-checks workflow set) — so "green on GitHub" for this branch is not yet established; only the
  rehearsal's local run of the same checks is.

---

## 15. Timeline

| Phase | Start | End | Elapsed |
|---|---|---|---|
| Precondition re-verification | 23:15 | 23:20 | ~5 min |
| Push + confirm | 23:22 | 23:23 | ~1 min |
| Log + summary write-up | 23:23 | 23:30 | ~7 min |

Total wall-clock: ~15 min. Biggest cost was none — this run had no conflicts to resolve and no tests
to re-run, since it was a pure replay of an already-verified rehearsal.
