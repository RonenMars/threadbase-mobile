# Components exempt from the mandatory-story pre-commit check — one path per
# line, exact match against the git-tracked path. See docs/storybook.md and
# scripts/check-story-coverage.js. Add a reason as a trailing comment.

# Store-driven host: renders nothing of its own, the visual is Toast (see Toast.stories.tsx).
components/ui/ToastViewport.tsx
# @gorhom/bottom-sheet composition — native-only primitive, matches the
# documented "don't story ... the bottom sheet" exclusion (docs/storybook.md).
components/alerts/StatusSheet.tsx
# Screen-sized list composition over stores, router and the quick-access sheet — the pieces (cards, rows, eyebrow) have stories.
components/sessions/now/NowList.tsx
# Store-driven host: announcements, CriticalDialog, and StatusSheet.
components/alerts/AlertHost.tsx
# Store-driven producer: warning lives on the status pill and Status sheet.
components/servers/HostPressureBanner.tsx
