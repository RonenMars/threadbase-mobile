## 20:17 | main
Created worktree fix/slow-sessions-fetch; raised SLOW_QUERY_THRESHOLD_MS 20s→60s in query-client.ts; added targeted cache update for cancelSession in useSessionActions.ts (avoids full sessions-eager re-fetch); PR #184 open.
I'll compress this session log to maximum density while preserving all facts, relationships, and verbs.

---

## 20:24 | main
Impl biometric lock (Face ID/fingerprint) `tb-mobile`: `useBiometricLock` hook, lock gate in `_layout.tsx`, settings toggle, i18n (en/he/ru/ar), iOS `NSFaceIDUsageDescription` + Android `USE_BIOMETRIC`/`USE_FINGERPRINT` perms → PR #185.

## 20:24 | main
Rebased PR #173 (`feat/stop-session-stream`): resolved `hooks/useSessionActions.ts` conflict (kept `respondToQuestion` + `stopSession`), fixed `SessionScreen.bubbleOnly.test.tsx` failure (added `stopSession` mock), discarded versionCode bumps, force-pushed; all CI ✓.

## 20:27 | fix/keyboard-hides-input
Fixed keyboard hiding input PR #180: RN `KeyboardAvoidingView` → `react-native-keyboard-controller` in `LiveConversationView.tsx` + `TerminalView.tsx`; added safe-area bottom padding `ChatComposer.tsx`; mocked `KeyboardAvoidingView` in integration tests; rebased → all CI ✓.

## 21:09 | fix/keyboard-hides-input
iOS simulator test: keyboard visible but chat scroll area doesn't adjust up—input visible, message bubbles cut off—`KeyboardAvoidingView` config in `LiveConversationView` needs review.

## 21:17 | fix/keyboard-hides-input
Switched `KeyboardAvoidingView` `'padding'` → `'height'` + added keyboard event listeners (`keyboardDidShow`, `keyboardDidChangeFrame`) auto-scroll `FlashList` to last message; added static `paddingBottom` gap `ChatComposer` ↔ keyboard.