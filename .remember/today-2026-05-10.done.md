I'll compress this work summary following maximum non-destructive compression rules.

## 20:51 | feat/align-with-jsonl-format-spec
Explored drag-and-drop server reordering: identified `ServersManageModal` entry point; confirmed `react-native-draggable-flatlist` installed w/ ref impl in `PromptQueueSheet.tsx`; designed multi-entry UX—organizer in `ServersManageModal`, `ServersStatus`, Filter/Sort toggle (lock/unlock order)—enables reordering across all view modes.