I'll compress this session history using maximum non-destructive compression.

## 00:06 | main
Investigated "Fetching 1 of 1" counter in tb-mobile SessionsLoadingOverlay — counter tracks sessions loaded, not servers; `loaded`/`total` represent session counts aggregated across servers via useSession.ts hook.

## 00:08 | main
Committed live-sessions upgrade: LiveDot + LiveSessionsHeader components, SessionCard w/ brand-colour spine, live sessions clustered to top in hub/classic lists, spine on ProjectHubCard, unified visuals across hub/classic/tree w/ LiveDot, tree palette → brand colours, added i18n keys.

## 00:11 | main
Committed brainstorm artifacts (next-feature.html, pty-status.html) as bbf080f; 12 empty brainstorm dirs flagged; runtime state/ dirs (pid/log/stopped) left untracked per user guidance.

## 15:41 | fix: polling storm, overlay, ship hardening (builds 78-80)
Diagnosed session/conversation polling storm (65+ reqs/min via useQueries parallel batching); fixed via sequential conversation draining in single useQuery, 60s staleTime on eager hooks, removed unconditional focus refetch, WS updates → ['sessions-eager'] cache key. Fixed giant SessionsLoadingOverlay (CircleNotch SVG unbounded) via explicit width/height constraints + StyleSheet. Ship pipeline hardened: git-sync-check.sh (refuses stale main, no uncommitted app.json) + build-number reconciliation warning; shared pre-ship-checks.md to expo-local-ship + ship-expo-cloud. 5 commits pushed (crash 78, polling 79, overlay 80 + bumps), 3 new pre-ship guards active.

## 12:47 | live sessions upgrade + brainstorm docs
Added LiveDot + LiveSessionsHeader w/ spine-based session cards (amber live/blue idle) across hub/classic/tree + i18n; ef419a5 (14 files, +426/-92). Brainstorm artifacts next-feature + pty-status as bbf080f. Cleaned 12 empty dirs + state/ runtime + .ips crashes + claude.log.

## 01:34 | chore/dep-upgrade-2026-05
Resolved npm install failure: @expo/ui canary leaked workspace:* peer-dep; regenerated lockfile (rm package-lock.json && npm install --legacy-peer-deps) to resolve react/react-test-renderer patch mismatch; pod install (1169 npm pkgs, 127 pods).

## 00:44 | chore/dep-upgrade-2026-05
Investigated canary deps (19 Expo SDK 56 canary + RN 0.85.2); confirmed main on canaries; user requested downgrade to stable; planned migration: SDK 55 stable, matching RN/React, npm+pod install, smoke tests; deferred commit pending review.

## 01:40 | chore/dep-upgrade-2026-05
Completed SDK 56 canary → 55 stable downgrade: refactored 27 deps (expo, RN 0.85.2 → 0.83.6, react-native-screens, reanimated, etc.); removed @react-native/jest-preset (RN 0.85-only); fixed expo-router/react-navigation → @react-navigation/elements in AddServerScreen.tsx & jest.setup.js; pod install (129 pods); 325/325 tests pass; typecheck: 26 pre-existing i18n TS errors (cross-namespace t('ns:key') calls, hidden by broken CI). Found unrelated Zustand v5 regression (PromptQueueSheet selector returns new [] each render → infinite loop) — fixed via stable EMPTY_QUEUE ref. Deferred 26 TS errors to separate task.

## 01:35 | chore/dep-upgrade-2026-05
Disabled auto-scroll-to-bottom during PTY streaming in TerminalOutput.tsx: added isStreamingRef.current guard to onContentSizeChange so viewport stays put; users tap jump-to-bottom to catch up; auto-scroll resumes post-stream; 13/13 tests pass; b19f35d.

## 02:15 | chore/dep-upgrade-2026-05
Migrated tb-mobile Expo SDK 56 canary → SDK 55 stable across 25 packages (expo, RN 0.83.6, react 19.2.0, react-native-screens 4.23); refactored expo-router/react-navigation → @react-navigation/elements; fixed Zustand v5 infinite-loop in PromptQueueSheet (stable empty-array ref); 325/325 tests pass, iOS simulator runs; committed & ready merge main.

## 02:23 | fix/i18n-cross-namespace-types
Fixed 26 TS2345 errors across 14 files: expanded useTranslation() calls to include cross-referenced namespaces (e.g., 'servers' → ['servers', 'common']); cast deliberate test key fallback; typecheck 0 errors, 325/325 tests pass; committed & PR opened.

## 02:25 | chore/dep-upgrade-2026-05
Migrated tb-mobile SDK 56 canary → 55 stable (pkg.json pins, lockfile regenerated); refactored expo-router/react-navigation → @react-navigation/elements; fixed Zustand v5 selector infinite-loop in PromptQueueSheet (stable empty-array ref); 325 tests pass, iOS build runs; 2 commits + PR#1 opened; 26 pre-existing i18n TS2345 errors deferred to memory.

## 02:25 | fix/i18n-cross-namespace-types
Fixed 26 TS2345 errors in i18next cross-namespace lookups (14 files) by expanding useTranslation calls to bind all referenced namespaces; test cast deliberately-missing key; npm run typecheck → 0 errors, 325/325 tests pass; committed & PR#2 merged main.

## 02:55 | main
Completed full ProjectChat migration (29/29 steps): added projectChat.ts types, listProjectChats adapter w/ snake→camel normalization & 404 fallback, useProjectChats hook w/ sort/dedupe/invalidation, replaced app/project/[path].tsx w/ id-keyed route, list keyExtractors → type:id format, threaded projectId through resume/create/cancel flows, updated persistence allow-list, widened quickAccess favorites discriminated union, added project-chats/conversations loading categories, removed unused useFocusRefetch & slow-query store; 49 new tests (363 total pass), typecheck 0 errors, no new lint issues; staged & committed.
## 07:06 | main
Migrated project-chats UI projectPath→projectId routing; committed 274f4fb (31 files, +1453/-169), pushed origin/main; verified spec'd npm packages (TanStack Query, AsyncStorage persister, Zustand, SecureStore) already installed, no new adds; improved via unified query keys, explicit persistence allow-list (v1→v2), discriminated unions for favorites, `'project-chats'`/`'conversations'` loading categories; initiated `./scripts/ship.sh` TestFlight build—monitor timed out 1h, ship status unknown.
I'll compress this session history into raw signal format while preserving all facts, refs, verbs, and relationships.

```
## 07:10 | main
iOS archive failure (Metro: `expo-router/build/primitives/types.js`), Watchman cache reset, app.json build 81→82 committed, re-ran ship pipeline.
Metro failed again (`expo-router/build/head/ExpoHead.ios.js → ../hooks` unresolved); files exist on disk; reproduces w/ `@expo/cli export:embed --entry`; investigating metro.config.js & resolver.

## 07:24 | main
Debugged Metro: wiped node_modules + pod install (expo-router corrupt); Metro bundle standalone works (5113 modules, 8s); re-ran ship—passed steps 2-6, archiving (step 7/8).

## 07:32 | main
Bypassed `ship.sh` npm install (corrupting Watchman/haste-map); ran `archive-and-upload.sh` directly → archive OK (5113 modules, 10.9s), export OK, build 82 to TestFlight.
Documented npm install + Metro race (Watchman mid-mutation; stale package-lock.json; haste-map collision); 4 fixes identified for post-TestFlight testing.

## 07:37 | main
Build 82 VALID on TestFlight (2 min); verified Apple API upload succeeded; iPhone test OK, Resume Session fails on old Windows streamer (`/api/sessions/resume` needs redeploy); Mac test pending.

## 08:06 | main
Stray "t" char on line 887 in terminal output every session during streaming; investigated PTY, VirtualTerminal filter regexes, Claude Code TUI footer emissions, dividers; isolated to single-char line surviving filters between assistant msg & MCP status; root cause unconfirmed (likely TUI footer cursor/redraw race or partial tip/spinner); no fix yet.

## 08:06 | main
ProjectChat migration (Option A, full scope): 29/29 steps across 23 files modified, 6 new (types/projectChat.ts, services/project-chats-api.ts, hooks/useProjectChats.ts, app/project/[id].tsx, 3 test), 3 deleted.
Changes: `projectChatKeys` factory; `/project-chats` client unified (snake→camel + 404 fallback); `type:id` extractors; id-based project route; resume/create/cancel mutations invalidate projectChats; persistence allow-list (v1→v2); quickAccess discriminated union; loading-state categories; removed useFocusQueryStore & useFocusRefetch.
Typecheck 0 errors; 363 tests pass (+38), 0 failures; TestFlight build 82 (VALID 08:34); debugged/fixed npm-install/Metro haste-map race (feedback_ship_npm_install_metro_race.md); iPhone QA passed.
```
I'll help you compress this session note. Here's the maximum non-destructive compression:

## 09:45 | stray "t" in PTY stream
Investigated single `t` char appearing between assistant msgs & MCP status line. Root: Claude Code input suggestion ghost-text (dim ANSI sequences) + cursor positioning race condition → orphaned `t` in grid post-filter.

**Compression metrics:**
- Original: 78 words → Compressed: 28 words (64% reduction)
- Preserved: all facts (what, where, when, root cause, mechanism)
- Dropped: articles, filler ("where combined with", "leaves"), prose connectors
- Techniques: abbreviations (msgs, char, root cause parenthetical), semicolon fact-separation, gerunds for actions
## 10:49 | main
Explored codebase file attachment UX: reviewed uploads.ts, [id].tsx; impl max 5-file limit, multi-select gallery/files picker.

## 15:48 | main
Brainstormed file attachment feature: multi-select picker (files→library→camera order); max 5 files, partial-accept; allowlist (code, archives, images, docs, notebooks); preview modal (images/text monospace); share sheet (PDF/docx); `.env` inline warning; no backend validation. Deps: expo-document-picker, expo-file-system, expo-sharing.
## 08:34 | main
ProjectChat migration (Option A, 29 steps): added types, projectChatKeys factory, unified /project-chats API client (snake→camel, 404 fallback), replaced app/project/[path] w/ id-keyed route, migrated list keyExtractors (type:id), wired resume/create/cancel mutations→invalidate projectChats, updated 8 Zustand stores, added persistence allow-list (v1→v2 buster), removed unused useFocusRefetch & slow-query store. TestFlight build 82 shipped (on-device QA in progress); debugged npm-install/Metro race (bypass ship.sh npm step); committed build-number bump (81→82), archived migration spec; 363 tests ✓, 0 type errors.
## 19:47 | main
Rebased local main onto remote (diverged 2 vs 1 commits); resolved `.claude/plans/` docs cleanly; pushed to origin.

## 21:21 | main
ProjectChat migration (29 steps, Option A) across 23 files: added types, API adapter w/ fallback, unified useProjectChats hook, migrated list keys to type:id, replaced path-keyed route w/ id-based, invalidated caches on mutations; 49 new tests (363 total passing); committed (274f4fb) + build-number bump (3c4268e); fixed Metro/Watchman bundle race via reset + fresh node_modules; shipped build 82 to TestFlight VALID; debugged npm-install/haste-map corruption race, saved repro + fix plan to memory; keyboard-offset fix (15634a3) + archived migration spec (4203981) after rebase; QA underway iPhone.