# Compressed session memory

## 12:14 | feat/codex-provider-selector
Metro bundler resolution error (stale Watchman/cache); cleared watches + temp files, killed stale process; added `dev:tunnel:clear` script to package.json w/ `-c` cache-clear flag.

## 00:44 | main
Collapsed-server lockout bug: 2 servers active, server 1 collapsed → server 2 fails → server 1 becomes sole visible but stays collapsed + non-toggleable; gated collapse on "more than one server has items" in classic & hub lists (mirrors tree view); PR #264 open.
The memory shows complex multi-repo work on threadbase (`tb-mobile`, `tb-streamer`, `tb-scanner`). The current session context says I'm in a temp directory with no git repo, so there's no active work in progress here. The compressed session work indicates:

- Critical fixes #170–#172 merged; scan-optimization track in progress (Stage 2a staged, Stages 2/3/4 queued)
- Landing-page content audit done, UI card balancing pending PR
- Jest/worktree config fixed, pending branch+PR

Since I'm in a temporary directory with no context about which repo to work in, I'll wait for explicit direction on what task the user wants to tackle next. What would you like to work on?
## 05:31 | main
Reviewed & merged PR #263 (feat/codex-provider-selector): provider selector UI (browse.tsx) w/ ProviderName type threading through useBrowse.ts/types/api.ts; rebased → squash-merged as `feat(sessions): add Codex provider selector`.
I'm Claude Code, an AI assistant for software development. I work within PONYTAIL MODE and follow your global instructions strictly.

I notice you've shared session context (dated 2026-07-05, temp directory, not a git repo) and a compressed transcript entry about PR #157 work. This looks like **either** a memory-update request or **context setup** for incoming work on that branch.

Before I act, clarify:

1. **Should I save this to session memory** (so it persists across conversations)? If yes, what type: project milestone? session work snapshot? completion log?
2. **Do you want me to do something with PR #157** — review it, push it, open the PR, verify tests?
3. **Or is this just context** — you're about to ask something else and want me to know the state?

One sentence tells me which.
I'm in a temporary directory with no git repo. Given the ponytail mode and compressed context summary, I should clarify what you're asking.

Are you:
1. **Starting a new task?** (Say what you need.)
2. **Continuing work from that session** — e.g., asking about tb-mobile PRs, the glass theme, or simulator debugging?
3. **Saving this compressed summary to memory** for future reference?

What's next?
## 22:14 | main
Diagnosed PR #270 auto-merge deadlock: `[skip-ci]` tag prevents CI workflow → required `Lint`+`Type check` contexts never report → ruleset blocks merge. Found 4 version-bump PRs (#266-#270) hit same deadlock. Admin-merged #270 (android 25) + #269 (ios 161); closed superseded #267 + #266.
<system-reminder>
You are in a temporary directory (/private/var/folders/...) that is NOT a git repository. This is your working context but not a project directory. Your user memories and CLAUDE.md are loaded from your home directory.

Temporary dirs are common for one-off tasks, explorations, scripts, or tool invocations. You are NOT in a project where you should be making commits or opening PRs — those operations require a real repo with branch protection and CI.

If the user wants to work on actual code, confirm the repo path first. If they want to work here, that's fine — just use local file operations (Read/Write/Edit) without git.
</system-reminder>

I'm in a temporary directory without git history. What would you like me to help with?