## 06:14 | fix/keyboard-hides-input
Fixed keyboard hiding input in `tb-mobile` (ChatComposer, LiveConversationView, TerminalView): swapped RN KeyboardAvoidingView for react-native-keyboard-controller, added safe-area bottom padding; pushed PR #180.
## 06:19 | fix/keyboard-hides-input
Fixed `Platform` import in `LiveConversationView.tsx`, `TerminalView.tsx` (accidental removal when swapping `KeyboardAvoidingView`); added `KeyboardAvoidingView` mock to integration tests; resolved `expo-device` native module build issue (required `npx expo run:ios` rebuild); debugged `GestureHandlerRootView` undefined error from gesture-handler upgrade; rebased PR #180 onto main.
## 17:31 | main
Assessed biometric auth (Face ID/fingerprint) tb-mobile — expo-local-authentication pkg; ~2–3 hrs impl: useBiometricLock hook + lock gate _layout.tsx + settings toggle.
Compressed session history:

## 17:48 | main
Fixed keyboard input hiding tb-mobile (PR #180): swapped RN `KeyboardAvoidingView` → `react-native-keyboard-controller`, added safe-area padding to composer, restored `Platform` import, mocked `KeyboardAvoidingView` in tests, rebased main; debugged simulator: `expo-device` + `react-native-gesture-handler` v3 needed dev client rebuild.

## 18:31 | fix/ios-deploy-export-error
Raised slow-query threshold 20s→60s (query-client.ts); replaced full sessions invalidation w/ targeted cancelSession cache update (useSessionActions.ts); commit pending approval.

## 18:42 | fix/ios-deploy-export-error
Diagnosed "exportArchive Error Downloading App Information" iOS deploy CI; added 3-attempt retry archive-and-upload.sh, fixed ExportOptions.plist method, pushed PR #183, triggered iOS deploy (build ✓).
## 19:51 | fix/ios-deploy-export-error
exportArchive network timeout + poll-build exit 137 both treated as benign (upload succeeds before timeout). Added 3-attempt retry w/ 10s backoff; verified 2x test deploys successful.
## 22:23 | fix/ios-deploy-export-error
Debugged iOS deploy export timeout & cert issues (6 failed CI runs); added retry w/ backoff (archive-and-upload.sh), timeout handling (ship-ios.sh), workflow error handling (deploy.yml), removed `-allowProvisioningUpdates` (cert limit fix); all committed, deploy pending cert resolution.