## 22:11 | fix/set-c-quick-access-ux
Removed bogus `keyboardVerticalOffset` (double-counted safe-area + header height); input bar was above keyboard. Committed, pushed, shipped to TestFlight via `/expo-local-ship`.

## 04:58 | fix/set-c-quick-access-ux
Shipped build 110 to TestFlight via `ship.sh`; resolved SSH port 22 timeout via git SSH-over-HTTPS (port 443); Bug 32 fix (Promise.allSettled multi-server sessions) confirmed in builds 109–110.
I'm Claude Code, ready to help with your software engineering tasks. I understand my instructions from CLAUDE.md — surgical changes, minimum viable scope, think before coding, and commit approval required.

I also have superpowers (skills) available. Looking at your context, I see you're working on a development branch (`fix/set-c-quick-access-ux`), just pushed keyboard positioning fixes, and had a TestFlight build upload interrupted mid-transmission.

What would you like help with right now?
Hi! I'm ready to help. Just checked the session context — no active tasks from previous work.

What would you like to accomplish?
I've compressed the context. Here's the summary:

## 18:57-20:56 | fix/set-c-quick-access-ux
Fixed `KeyboardAvoidingView` in app/session/[id].tsx by removing bogus `keyboardVerticalOffset={insets.top + 52}` (double-counted safe-area + header); committed b740292, pushed to origin. TestFlight build 111: upload failed twice (~90s in during SPI handshake), succeeded in foreground. Build now valid/installable but keyboard positioning regressed — hidden in new sessions, too high on resume, missing in some cases. Root cause: fix overcorrected; needs live simulator measurement to determine correct offset; user requested Maestro test automation.

---

This preserves all facts (the bug, the fix, the artifact hashes, the deployment issues, the regression, and the next step) while cutting ~60% of the prose.
## 22:43 | fix/set-c-quick-access-ux
Reproduced keyboard-layout bug iOS simulator (Maestro); input bar floats 150pt too high; created `waiting_input` fixture + repro flow `keyboard_session_input.yaml`; root cause: KAV frame measured from window top instead of below in-content header; delegated offset-sweep validation to background agent.