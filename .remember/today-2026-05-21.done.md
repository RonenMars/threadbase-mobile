## 22:22 | main
Debugged oversized MessageBubble; identified 11,500+ char XML-entity messages → 10 lines, ~6,900pt height via soft-wrap. Recommended char-budget collapse + entity decode fix.

## 15:45 | main
Fixed MessageBubble.tsx whole-bubble text trimming: moved collapse before code-fence split; caps message ~600 chars/10 lines + single "Show all" toggle.
I'll compress this session history following maximum non-destructive compression rules.

## 15:58–18:55 | main
Implemented Bug 1 (conversation loader min-display) with `useMinDisplayTime` hook + 1.2s skeleton overlay; fixed pre-existing SkeletonBox NativeWind bug (className → inline backgroundColor). Investigated & fixed message bubble height bug: giant blue bubbles caused by rendering full HTML-entity-encoded XML text (11k+ chars) across split code-fence parts; solution: collapse whole-text at decode stage (600 chars or 10 lines) before splitting w/ single "Show all N lines" toggle. Removed drain-all-older-messages effect to fix Bug 4: list mounts at bottom of last page (80 msgs) w/ zero scroll animation, older pages backfill on upscroll. Refined scroll: replaced 600ms timer w/ settle-detect for auto-scroll on full content render; added `maintainVisibleContentPosition` to FlatList (gated to prevent init conflicts); replaced manual `scrollToOffset` w/ `scrollToEnd` for "Bottom" btn; wired `onScrollBeginDrag` to track user interaction; tested on 268-msg conversation—lands correctly at bottom on load, "Bottom" btn jumps to end w/o recursive backfill. Commits: 07f4593, 1e62f0b, 7e197ab.
## 19:46 | main
Fixed scroll-to-bottom: settle-based repeated scrolling w/ 1.5s delay + content-size tracking; ensures list scrolls end after large message batches finish layout.

## 21:20 | main
Fixed scroll-to-bottom: added 400ms-delayed animated final scroll after layout settles, catches tail-end content, prevents short-stop when "Bottom" button reveals more rows; committed fix(bug-4), pushed.