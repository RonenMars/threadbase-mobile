Got it. The session-work memory shows prior cross-repo PTY→streamer→mobile live chat work. Now I see a new transcript (fix/ios-deploy-export-error) unrelated to that. Let me understand the current state by checking if this is a new working directory:

Current working directory is `/private/var/folders/9r/c18379_13_q6tl6x_k9bhyqh0000gn/T` which is NOT a git repo. This appears to be a temp directory. Without seeing active git state, I'll wait for your next action.

**Status:**
- Prior work: PTY live-chat cross-repo fix (both branches pushed, streamer running)
- Transcript shows iOS deploy work (build compliance, ASC sync issues) — but no git context available yet
- Current dir is temp (not repo)

What's next? Are you picking up from one of these branches, or starting fresh?
## 01:42 | fix/ios-deploy-export-error
Switched iOS App Store upload from `xcodebuild -exportArchive (method: app-store-connect)` to `xcrun altool --upload-app`; separated export/upload in `ExportOptions.template.plist` + `archive-and-upload.sh`; bypasses Xcode 26 silent rejection.
## 10:44 | fix/ios-deploy-export-error
Altool auth: copied p8 key to `~/.appstoreconnect/private_keys/` in `bootstrap-ios-signing.sh`. Upload succeeds; archive creates new "Created via API" dev certs due to `CODE_SIGN_STYLE=Automatic` on ephemeral CI runners. Root: Xcode creates fresh certs on new machines instead of importing `.p12`. Fix: switch to Manual signing + Distribution cert + provisioning profile secret.
I need to compress this into minimal signal format. Applying maximum density while preserving ALL facts, refs, verbs, relationships.

## 10:46 | fix/keyboard-hides-input
Swapped RN `KeyboardAvoidingView` → `react-native-keyboard-controller` + safe-area padding in ChatComposer; fixed missing `Platform` import LiveConversationView & TerminalView; added KeyboardAvoidingView mock to integration tests; rebased PR#180→main.

## 10:55–11:21 | fix/ios-deploy-export-error
xcodebuild silent build rejections: wrong `distributionBundleIdentifier` in ExportOptions, internal ASC upload path dropping builds. Switched to explicit `xcrun altool` upload; fixed altool AuthKey path (bootstrap: ~/.appstoreconnect/keys/ → altool expects ~/.appstoreconnect/private_keys/). Changed signing Automatic→Manual: updated bootstrap-ios-signing.sh export cert/profile UUIDs, archive-and-upload.sh use manual style, ExportOptions.template.plist profile UUID placeholder, deploy.yml use new secrets (IOS_DIST_CERT_P12_B64, IOS_DIST_CERT_PASSWORD, IOS_PROVISION_PROFILE_B64, IOS_PROVISION_PROFILE_UUID). User set all 4 GitHub secrets from Xcode ASC profile 59cc6cc6; pushed code changes. Deploy failed: bootstrap skip left IOS_PROVISION_PROFILE_UUID empty in .env.signing; removed skip logic. Retried: passed UUID env var directly to ship-ios step (bootstrap check was skipping .env.signing source). Confirmed run succeeded but archive unsigned; root cause: workflow using cached old bootstrap script before UUID env var fix was pushed.

## 11:27 | fix/slow-sessions-fetch
Raised SLOW_QUERY_THRESHOLD_MS 20s→60s; replaced full sessions-eager invalidation on cancel w/ targeted cache patch (session→idle status); PR#184 open.

## 11:31–11:33 | main
Merged PR#183: iOS deploy pipeline final: xcodebuild internal upload→explicit xcrun altool; Automatic signing→Manual (Distribution cert + App Store provisioning profile, stopped auto-creating "Created via API" certs); fixed .p8 key path & bootstrap env vars in deploy.yml; build 148 succeeded in TestFlight. Created worktree fix/slow-sessions-fetch: SLOW_QUERY_THRESHOLD_MS 20s→60s query-client.ts; replaced full invalidation w/ targeted cache patch cancelSession useSessionActions.ts; 12/12 tests pass; pushed PR#184.

## 11:44 | main
Researched react-native-boost lib: confirmed compatible Expo SDK56 + RN0.85.3; flagged potential React Compiler Babel transform interaction; ready dev testing.
Now compressing to handoff format:

## 11:53 | main
Device-setup tutorial pointer added to README; hardcoded iPhone UDID replaced w/ placeholder in `docs/dev-on-iphone-17-pro.md`; USB→Wi-Fi transition step added.

## 11:59 | main
Keyboard input hiding bug fixed in `tb-mobile`: `KeyboardAvoidingView` → `react-native-keyboard-controller` variant; safe-area padding added to `ChatComposer`; `Platform` import restored; `KeyboardAvoidingView` mock stub added to integration tests (PR #180).

## 12:01 | main
Biometric lock (Face ID/fingerprint) impl on new worktree: `useBiometricLock` hook w/ 10s grace period + AppState listener; `BiometricLockGate` in `_layout.tsx`; Privacy toggle in settings; i18n (en/he/ru/ar); iOS `NSFaceIDUsageDescription` + Android biometric perms in `app.json` (PR #181).

## 12:06–12:14 | feat/drop-recents-popular
Switched worktree; added device-setup README pointer + "Switch to wireless" USB→Wi-Fi section in `dev-on-iphone-17-pro.md`; created parallel `docs/redact-device-udid` branch from main, replaced hardcoded UDID/CoreDevice ID/name w/ placeholders; both committed & pushed; rebased feat/drop-recents-popular onto main, force-pushed, self-reviewed.

## 12:18–12:21 | feat/live-chat-with-flow-fixes
Worktree created & rebased onto `origin/main` (53 commits, conflicts in ws-client.ts + useConversationStream.ts resolved via `--reapply-cherry-picks -X ours`); verified branch adds conversation_event WS type, useConversationStream hook, LiveConversationView; backend commit 7ed083d analyzed — 6 new WS events + REST endpoint found; mobile handles 5/6 (missing `conversation_events` batched variant); Feature 32 added to @docs/ROADMAP.md for batched WS handler.

## 12:40 | main
iOS deploy pipeline fixed: Xcode auto-managed → manual signing w/ Distribution cert + App Store profile; xcodebuild export method `app-store-connect` → `app-store` + explicit `xcrun altool` upload; cert/profile mismatches resolved; fix/ios-deploy-export-error merged; builds 148+ now in TestFlight via corrected workflow (no more "Created via API" dev certs).

## 12:57 | feat/live-chat-with-flow-fixes
Rebased onto latest `origin/main` (53 commits, 43 dropped as upstream; conflicts resolved); backend commit 7ed083d coverage verified except `conversation_events` batched variant; Feature 32 added to ROADMAP.md; pushed at de7682dd.

---

**Next session:** Resume from `feat/live-chat-with-flow-fixes` (de7682dd) — 92–93 tests passing; needs `conversation_events` batched WS handler (Feature 32) or merge as-is if not critical path.
Got context. You're on feat/live-chat-v2, rebased onto main with conflicts resolved. Squashed 55 commits. Last action: force-pushed after removing RawKeyBar.

What's next? — PR merge, test run, continue implementing streamer relay?
# Session compressed

## 17:16–18:14 | iOS deploy pipeline fix + build version bump
Fixed iOS deploy: manual signing (Distribution cert/profile), `xcrun altool` (replaced xcodebuild internal upload), removed cert proliferation (was 8+/run). Build 149 blocked by Apple version collision (altool rejected despite absent from ASC UI). Bumped app.json buildNumber→150, pushed chore/bump-ios-version-150, workflow 28177163647 in progress (iOS archive). Merged PR #183 (feat/live-chat-v2).

---

What's next?
## 19:14 | main
Rebased `feat/live-chat-with-flow-fixes` onto latest main (43 commits upstream, 10 replayed); pushed; created `feat/live-chat-with-flow-fixes-pre-rebase` snapshot; opened PR #187; squashed branch into single commit on `feat/live-chat-v2`, rebased onto main, opened PR #188; removed `RawKeyBar` usage from views (component retained); added ROADMAP Feature 32 (batched `conversation_events` WS handler); committed & pushed both PRs.
I'm reading compressed history: iOS deploy fixes (19:17–20:32), then a rebase/squash workflow on feature branches (20:32). Noting the pre-rebase snapshot (#187) as insurance, squashed v2 branch (#188) with conflict resolution favoring upstream, component cleanup (RawKeyBar dropped from usage but kept in code).

Memory context shows feat/live-chat-with-flow-fixes in progress (92–93 tests passing). Current date: 2026-06-25.

**What's your next move?** CI status on #188? Waiting for review? Picking up a different task?