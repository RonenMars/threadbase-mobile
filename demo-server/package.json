{
  "name": "threadbase-demo-server",
  "private": true,
  "version": "1.0.0",
  "description": "Public demo of Threadbase — runs a real @threadbase/streamer release against a curated seed corpus. See README.md.",
  "scripts": {
    "deploy": "fly deploy"
  },
  "engines": {
    "node": ">=24"
  }
}
