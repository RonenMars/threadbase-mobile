node_modules/
.expo/
dist/
build/
npm-debug.*

# Local scratchpad — brainstorm + restored stashes, never committed
.scratch/

# Real MCP config with developer-specific credential paths — see .mcp.example.json
.mcp.json

# Local ship build logs and audit artifacts — large + ephemeral
logs/

# Maestro E2E run artifacts (per-step screenshots, debug bundles, view trees)
e2e/_artifacts/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
# android/ and ios/ are tracked — CNG is not used in this project
# (prebuild does not run in the ship pipeline; native dirs are modified directly)
android/.kotlin/
.env.*
.env
# Keep committed templates — real env files stay ignored.
!.env.example
!fastlane/.env.example

# macOS metadata
.DS_Store

# Crash logs
*.crash

# Local upload staging
.threadbase-uploads/

# Local archives
ThreadbaseMobile.zip

# Fastlane build artifacts
fastlane/report.xml
fastlane/Preview.html
fastlane/test_output/
fastlane/screenshots/
*.ipa
*.dSYM.zip

# Bundler local install
vendor/bundle/
.bundle/
Gemfile.lock

# Local Claude Code settings — user-specific permissions / preferences
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/worktrees/
.worktrees/

# Local planning notes and brainstorm working state — not part of the public surface
.claude/plans/
.superpowers/

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli
# Local-only assets from App Store submission prep (PII / large)
/screenshots/
/screenshots-ipad/
/.playwright-mcp/
/threadbase-ai-site.png
/threadbase-sh-live.png
/uspto-threadbase.png

# Local status notes that contain private credentials — do not commit
/docs/app-store-submission-status-*.md
/docs/kickoff-homepc-validation.md

# Cloudflare tunnel credentials (generated by `cloudflared tunnel create`)
# The config template (.cloudflared/config.yml) IS committed; credentials are not.
.cloudflared/*.json
