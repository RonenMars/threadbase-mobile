source "https://rubygems.org"

# Pin fastlane for reproducible TestFlight uploads across machines.
gem "fastlane", "~> 2.224"

# Podfile.lock was generated with CocoaPods 1.17.0; deploy CI must use the same
# resolver/checksum behavior when running `pod install --deployment`.
gem "cocoapods", "1.17.0"
