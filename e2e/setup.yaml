# Reusable setup flow. On first run (no stored credentials) walks the
# 7-step onboarding carousel; on subsequent runs the app lands on the hub
# directly because `expo-secure-store` survives `launchApp: clearState`.
# Either way, the flow ends with hub-screen visible.
# Requires: mock server running on localhost:7071

appId: com.ronenmars.threadbase
---
- launchApp:
    clearState: true

# If the welcome step is visible, walk the entire onboarding carousel.
# Otherwise the app already has credentials cached and is on the hub.
- runFlow:
    when:
      visible:
        id: "onboarding-welcome-cta"
    commands:
      # Step 1 / WELCOME — "Pull a thread. Watch it weave."
      - tapOn:
          id: "onboarding-welcome-cta"

      # Step 2 / THEME — pick default theme, continue
      - tapOn:
          id: "onboarding-theme-cta"

      # Step 3 / VALUE PROP — explanatory diagram
      - tapOn:
          id: "onboarding-valueprop-cta"

      # Step 4 / CONNECT — paste credentials, fill URL + token, handshake
      - tapOn:
          id: "onboarding-connect-paste-card"

      - tapOn:
          id: "onboarding-connect-url-input"

      - inputText: "http://localhost:7071"

      - tapOn:
          id: "onboarding-connect-token-input"

      - inputText: "mock-key-123"

      - tapOn:
          id: "onboarding-connect-handshake-cta"

      # iOS may show a "Save Password?" dialog after secureTextEntry input
      - runFlow:
          when:
            visible: "Not Now"
          commands:
            - tapOn: "Not Now"

      # Step 5 / NOTIFICATIONS — wait for handshake to advance, then skip
      - extendedWaitUntil:
          visible:
            id: "onboarding-notifications-cta"
          timeout: 8000

      - tapOn:
          id: "onboarding-notifications-cta"

      - runFlow:
          when:
            visible: "Not Now"
          commands:
            - tapOn: "Not Now"

      # Step 6 / TOUR — 3 concepts, tap CTA each time
      - tapOn:
          id: "onboarding-tour-cta"

      - tapOn:
          id: "onboarding-tour-cta"

      - tapOn:
          id: "onboarding-tour-cta"

      # Step 7 / DONE — enter the app
      - tapOn:
          id: "onboarding-done-cta"

- extendedWaitUntil:
    visible:
      id: "hub-screen"
    timeout: 8000
