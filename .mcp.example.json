{
  "_comment": "Template. Copy to .mcp.json (gitignored) and replace the GOOGLE_APPLICATION_CREDENTIALS placeholder with the absolute path printed by scripts/fetch-play-credentials.sh. See docs/google-play-mcp-setup.md.",
  "mcpServers": {
    "google-play-mcp": {
      "type": "stdio",
      "command": "uvx",
      "args": [
        "google-play-mcp"
      ],
      "env": {
        "GOOGLE_APPLICATION_CREDENTIALS": "<absolute-path-to-play-console-sa.json>"
      }
    },
    "gradle": {
      "type": "stdio",
      "command": "jbang",
      "args": [
        "run",
        "--quiet",
        "--fresh",
        "gradle-mcp@rnett"
      ],
      "env": {}
    }
  }
}
