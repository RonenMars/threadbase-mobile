/** Shared accessibility / Dynamic Type constants for dense surfaces. */
export const MIN_TOUCH_TARGET = 44

/** Cap Dynamic Type on monospace terminal/diff so layout stays usable. */
export const MAX_FONT_SIZE_MULTIPLIER_MONO = 1.35

/** Primary UI text can scale further while remaining readable. */
export const MAX_FONT_SIZE_MULTIPLIER_UI = 2
